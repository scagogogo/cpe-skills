run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - govet

linters-settings:
  staticcheck:
    checks: ["-SA5011"]

issues:
  exclude-use-default: true
  max-issues-per-linter: 50
  max-same-issues: 3
